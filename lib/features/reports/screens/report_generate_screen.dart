import 'package:flutter/material.dart';

class ReportGenerateScreen extends StatelessWidget {
  const ReportGenerateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ReportGenerateScreen')),
      body: const Center(
        child: Text('ReportGenerateScreen'),
      ),
    );
  }
}
