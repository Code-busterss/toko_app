## Summary of findings vs. the spec

Before planning, the codebase differs from the prompt's assumptions in ways that change the work:

- **Package is `toko_app`, not `tokoapp`** — all imports use `package:toko_app/...`.
- **Feature 1 (Payments & Credit) is already fully built** (receive-payment wizard, credit ledger, PDF receipt, notifiers). Only needs `notes`, a transaction wrapper, and minor model alignment — NOT a rewrite (per your decision).
- **DB tables mostly exist**; only `purchaseitems` is truly new. Existing `purchases` uses a JSON `products` column — migrating to normalized (per your decision).
- **Several "broken" stock items are already correct** (StockScreen watch, history ASC sort, repo ORDER BY). Real bugs are narrower.
- **`path_provider` is imported by 3 services but missing from `pubspec.yaml`** — real compile/runtime bug.
- **Two router files exist**; `lib/core/app_router.dart` is canonical (used by `main.dart`), `lib/core/router/app_router.dart` is a dead duplicate — delete it.
- **Biometric + PIN already work** via `FingerprintService` + `PinService`; only the Login screen's buttons are placeholders, and `app_startup.dart` (dead code) uses `Navigator` + `setState`.
- **`setState` is strict-zero** per your decision — *all* of it (including obscure-password/date-picker/save flags) routed through Notifier state.
- **Firebase Auth stays for login/register**; register currently writes to Firestore → move business data to SharedPreferences.

---

## Phase 0 — Foundation & corrections (do first)

**0.1 `pubspec.yaml`** — add: `path_provider: ^2.1.4`, `flutter_local_notifications: ^17.2.2`, `package_info_plus: ^8.0.0`. (Removes the broken-service imports.)

**0.2 `lib/core/database_service.dart`** — bump `version: 2 → 3`.
- **`_onCreate`** (fresh installs): add `createdAt TEXT NOT NULL` to products, customers, orders, payments, suppliers, purchases, expenses, stock_adjustments; add `customerName TEXT` + `notes TEXT` to payments; add `supplierName TEXT, subtotal REAL, transportCharges REAL DEFAULT 0, otherCharges REAL DEFAULT 0, notes TEXT` to purchases; add `totalPurchases REAL DEFAULT 0, pendingAmount REAL DEFAULT 0, createdAt TEXT NOT NULL` to suppliers; create `purchaseitems` table (`id, purchaseId, productId, productName, quantity, buyingPrice, total`).
- **`_onUpgrade`**: `if (oldVersion < 3)` — `ALTER TABLE ... ADD COLUMN` for every additive column above (guarded, additive-only, non-breaking), plus `CREATE TABLE IF NOT EXISTS purchaseitems`.
- **`clearAll()`**: add `purchaseitems` to the list.

**0.3 `lib/core/constants/constants.dart`** — add SharedPreferences key constants (`keySession`, `companyName/phone/address/logo`, `currency`, `invoicePrefix`, `invoiceStartNo`, `darkMode`, `pinCode`, `lastBackupDate`, `recentSearches`), route constants (`routeNotifications`, `routeSearch`, `routeProfitLoss`), currency list (`Rp, ₨, $, etc.`).

**0.4 `lib/core/theme/theme.dart`** — add a shared `appInputDecoration(String label, {Icon? icon, Widget? suffix})` helper enforcing the spec's TextField style (filled white, grey border, blue focused border). Used everywhere.

**0.5 Delete** `lib/core/router/app_router.dart` (dead duplicate).

**0.6 `lib/main.dart`** — initialize DB + `flutter_local_notifications` PluginUtils before `runApp`; wrap in `ProviderScope`.

---

## Phase 1 — Fix existing code (Part 1 of spec)

### 1.1 Auth
- **`auth_notifier.dart`**: `register()` stops writing to Firestore; writes `companyName/phone/email/uid/createdAt` to SharedPreferences instead. `login()`/`register()` set `keySession=true` on success; `logout()` clears it + clears session.
- **`splash_screen.dart`**: ConsumerStatefulWidget; reads `keySession` from SharedPreferences → Dashboard (then PIN gate) or Login. Removes arbitrary 3s timeout (use ~1.2s for animation).
- **`pin_provider.dart`**: no change to logic; add `hasSession`/`clearSession` helpers; PIN stored plaintext stays (already the case).
- **`login_screen.dart`**: remove `_obscurePassword` setState → local `Notifier<bool>` (`passwordObscureProvider`). Wire **Biometric button** to `FingerprintService.authenticateWithBiometrics()` → on success `context.go(dashboard)`. **PIN button** → `context.go('/pin-lock')` (real). On login success: if `pinService.isPinSet()` → push pin-lock, else dashboard. Apply shared input decoration.
- **`register_screen.dart`**, **`forgot_password_screen.dart`**, **`pin_lock_screen.dart`**: route all setState (obscure flags, emailSent, biometric-available, numpad state, loading/error) through Notifier state. Apply shared input decoration.
- **`app_startup.dart`**: delete (dead code, replaced by splash-driven flow). Splash + login handle PIN gate.

### 1.2 Stock (real issues only)
- **`stock_adjustment_screen.dart`**: remove the 5 `setState` calls (date picker result, `_isSaving`, product/type selection) → a `StockAdjustmentFormNotifier`. Migrate `withOpacity` → `withValues`.
- **`stock_history_screen.dart`**: keep (already Riverpod, already ASC). Verify running-balance starts from `previousBalance` not just current stock; document via code.
- **`stock_list_screen.dart`**, **`stock_detail_screen.dart`**: delete (plain-stateless stubs superseded by `stock_screen`/`stock_history_screen`).
- **`stock_adjustment_model.dart`**: leave `date` as String (documented exception); add optional `reason`/`notes` if needed.

### 1.3 Payments — adapt to spec (NOT rewrite)
- **`payment_model.dart`**: add `String? customerName`, `String? notes`. Keep `type` as enum `incoming/outgoing/refund` (the working screens depend on it) — no data-format break.
- **`payment_repository.dart`**: new `recordPaymentAtomic(Payment, {int? orderId, double orderPaidAmount})` wrapping insert-payment + `updateCustomerBalance` + `updateOrderPaidAmount` in **one `db.transaction`** (currently 3 separate calls). Keep old `recordPayment` for back-compat.
- **`payment_notifier.dart`**: call `recordPaymentAtomic` instead of the 3 separate calls.
- Screens already complete; apply shared input decoration + verify loading/error states.

### 1.4 Transactions + createdAt everywhere
- Wrap multi-step repos in `db.transaction`: `PaymentRepository.recordPaymentAtomic` (above), `PurchaseRepository.createPurchase` (insert purchase + items + stock + supplier totals), `ExpenseRepository` (single-write, no txn needed).
- Add `createdAt` fields to: Product, Customer, Order, Payment, Supplier, Purchase(+PurchaseItem), Expense models (nullable in `fromMap` for legacy rows, auto-set `DateTime.now().toIso8601String()` in `toMap` when null).

---

## Phase 2 — Build remaining features (Part 2 of spec)

All features follow established conventions: plain Dart models with `toMap/fromMap`, zero-arg repositories using `DatabaseService.instance.database`, Riverpod `Notifier`/`NotifierProvider`, ConsumerWidget/ConsumerStatefulWidget, zero setState (all via Notifier state).

### 2.1 Suppliers (Feature 2)
- `supplier_model.dart` — add `totalPurchases`, `pendingAmount`, `createdAt` to existing.
- `supplier_repository.dart` — CRUD + `getSupplierStats` (totals from purchases/payments).
- `suppliernotifier.dart` — `SupplierListNotifier` (search/filter), `SupplierFormNotifier`.
- Replace 3 stub screens: `supplier_list_screen.dart` (search + cards + pending badge), `supplier_add_edit_screen.dart` (validated form), `supplier_detail_screen.dart` (info card + tabs: Purchase History / Payment History / Pending summary).

### 2.2 Purchases (Feature 3) — normalized
- `purchase_item_model.dart` (new) + `purchase_model.dart` (add `supplierName, subtotal, transportCharges, otherCharges, notes, createdAt`; keep `products` JSON for back-compat but write to `purchaseitems` too).
- `purchase_repository.dart` — `createPurchase` atomic txn (purchase + items + increase product stock + update supplier totalPurchases); list/detail queries JOIN purchaseitems.
- `purchasenotifier.dart` — list + form notifiers.
- Replace 3 stub screens: `purchase_list_screen.dart` (search by supplier/invoice, date filter, totals), `purchase_add_edit_screen.dart` (supplier dropdown, date picker, invoice no, add-products with running total, charges, grand total), `purchase_detail_screen.dart` (full info + items + print purchase-order PDF).

### 2.3 Expenses (Feature 4)
- `expense_model.dart` — add `createdAt`.
- `expense_repository.dart` — CRUD + `getTotals(today/week/month)` + `getByCategory`.
- `expensenotifier.dart` — list (filter by category + date range) + form.
- Replace 2 stub screens: `expense_list_screen.dart` (summary cards Today/Week/Month, category chips, date-range picker, swipe-to-delete via `Dismissible`), `expense_add_edit_screen.dart` (category dropdown, amount, date picker, notes).

### 2.4 Reports (Feature 5) + Profit & Loss (Feature 6)
- `reportnotifier.dart` — raw-SQL aggregations for 9 report types (daily/monthly sales, customer, product, stock, purchase, expense, pending payments, P&L) + date-range filter.
- `reportsscreen.dart` — tabbed (9 tabs), each with date-range filter + Export-Excel button (reuse `ExcelService` pattern, save to Downloads via `path_provider`).
- `profitlossscreen.dart` — period selector, 5 summary cards, `fl_chart` BarChart (Revenue vs Expenses, 6 months), best-selling + slow-moving lists.

### 2.5 Analytics (Feature 7)
- `analytics_screen.dart` — `fl_chart` LineChart (12-mo revenue), BarChart (monthly profit), 2 PieCharts (top products, expenses by category), customer-growth, month/year picker, dead-stock list. Add `analyticsnotifier.dart`.

### 2.6 Settings (Feature 8)
- `settingsnotifier.dart` — `SettingsNotifier` over SharedPreferences (all keys from spec) + theme-mode provider (wired into `main.dart` MaterialApp).
- `settingsscreen.dart` — sections: Business Info (text fields + logo via imagepicker→base64), Invoice Settings (prefix + start no), App Settings (currency dropdown, dark-mode toggle), Security (set/change PIN, biometric toggle → reuse `PinService`/`FingerprintService`), Backup & Restore, About (`package_info_plus`).
- `backup_restore_screen.dart` — Export: read all tables → JSON `{"version":1,"date":...,"<table>":[...]}` → save `tokobackup_YYYYMMDD.json` to Downloads → Share button → save `lastBackupDate`. Import: filepicker → validate → preview counts → confirm → clear all tables → insert all → progress indicator.

### 2.7 Global Search (Feature 9)
- `searchnotifier.dart` — debounced (300ms `Timer`) search across products/customers/orders/suppliers/payments/expenses; recent-searches (last 5 in SharedPreferences).
- `searchscreen.dart` — autofocus search bar, sectioned results with headers, tappable rows → detail screens, empty state, recent searches chips.

### 2.8 Notifications (Feature 10)
- `notificationservice.dart` — init `flutter_localnotifications`; on app start check: low-stock (stock ≤ minStock), payment-due (balance>0 & last order >7d), backup-reminder (lastBackupDate >7d); save history JSON list to SharedPreferences.
- `notificationsscreen.dart` — list past notifications (icon, title, date, dismiss).

---

## Phase 3 — Wiring

- **`app_router.dart`**: replace all `_ComingSoonScreen` placeholders with real screens (suppliers, purchases, expenses, reports, analytics, settings, profile, backup-restore, search, notifications, profit-loss, order/customer/purchase/supplier detail). Fix route param wiring (int IDs). Wire detail/edit routes.
- **`main_shell.dart`**: keep 5 tabs (Dashboard, Products, Customers, Orders, Settings) but ensure Settings tab opens real settings; quick-action buttons on Dashboard/Settings that currently have empty `onTap` get real navigations.
- **`main.dart`**: theme-mode from `SettingsNotifier`; init `NotificationService` after DB ready.
- **`constants.dart`**: add any final missing route/path constants.

---

## Execution approach
- **One file at a time, complete code, no placeholders/TODOs** (per your rule 10).
- Order: Phase 0 (foundation) → Phase 1 (fixes) → Phase 2 features in dependency order (suppliers → purchases → expenses → payments-enrichment → reports/P&L → analytics → settings → search → notifications) → Phase 3 wiring.
- After each logical group, run `flutter analyze` to catch errors early.
- Match existing conventions exactly (camelCase columns, `toMap/fromMap`, `Notifier` providers, `DatabaseService.instance.database`).

## Risks / notes
- **DB migration is additive-only** (ALTER TABLE ADD COLUMN) — existing data preserved; `createdAt` nullable for legacy rows.
- **Firebase Auth stays** for login/register; Firestore write in register removed. No data migration off Firestore needed (business data wasn't stored there yet in a way screens depend on).
- **Strict-zero setState** adds a small Notifier per screen for ephemeral flags — more boilerplate but literal compliance.
- **`pdf`/`printing`/`excel` services already work** and will be reused for purchase-order PDF and report exports.